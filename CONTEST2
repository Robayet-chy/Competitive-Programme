#include <iostream>
#include <vector>
using namespace std;

vector<int> primes;
vector<bool> check(100000000, false);

void sieve(int n) {
    check[0] = true;
    check[1] = true;
    for (int i = 2; i * i <= n; i++) {
        if (!check[i]) {
            for (int j = i * i; j <= n; j += i) {
                check[j] = true;
            }
        }
    }

    for (int i = 0; i <= n; i++) {
        if (!check[i]) {
            primes.push_back(i);
        }
    }
}

int main() {
    sieve(100000000 - 1);

    for (int i = 0; i < primes.size(); i++) {
        if (i % 100 == 0) {
            cout << primes[i] << endl;
        }
    }
return 0;
}
